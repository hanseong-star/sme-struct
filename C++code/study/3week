arrays and linked lists

같은 타입을 가진 요소들의 집합
메모리상의 연속해서 존재, 주소로 쉽게 컨트롤

static 메모리(아래에서 위로)
메모리를 할당
데이터를 이상한 메모리에 넣으면 
read할떄 에러가 발생함. 주의

#알아두면 좋은것 TDD

dynamic 메모리를 해야함(위에서 아래로)

memcpy
   메모리 카피

메모리는 1차원
n차원 배열은 메모리에 어떻게 저장될까
행을 우선으로 저장됨
Row-major-order

진수변환
0x0000 1000 = 0x08 #16진수
0x0000 1100 = 0x0c #16진수

게임 데이터 내림차순으로 저장

*class 
*주소




